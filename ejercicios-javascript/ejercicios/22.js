function resta(x, y) {
  // La función recibe dos argumentos "x" e "y" que son números.
  // Retorna el resultado de la resta.
  // Tu código:
  var resultado_resta = x - y;
  return "EL RESULTADO ES: " + resultado_resta;
}

console.log(resta(78,40));

module.exports = resta;
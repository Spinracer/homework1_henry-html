function suma(x, y) {
  // La función recibe dos argumentos "x" e "y" que son números.
  // Retorna el resultado de su suma.
  // Tu código:
  var resultado_suma = x + y;
  return "EL RESULTADO ES: " + resultado_suma;
}
console.log(suma(9,1));
module.exports = suma;